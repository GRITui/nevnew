# Backlog

Pending follow-ups for the NevNew (นิวนิว) stack. Add dated entries as new items come up.

## Tracking

Sprint/feature work is now tracked on the GitHub Project board, not just this file:
https://github.com/users/GRITui/projects/7 ("NevNew - Personal Assistant"), repo `GRITui/nevnew`.
This file stays for infra/ops notes that don't fit as issues.

## Open

- **Headless `docker compose build` fails on macOS Keychain (2026-09-13)** — buildx calls `docker-credential-osxkeychain` for registry metadata even on public images; in non-interactive sessions (cron, headless agent) the keychain is locked and the build fails. Workaround that worked: stub helper + keychain-free docker config — `mkdir -p /tmp/no-keychain-bin /tmp/docker-nokeychain`, stub `docker-credential-osxkeychain` returning `{}`, `DOCKER_CONFIG` copy of `~/.docker/config.json` minus `credsStore`/`credHelpers` plus symlinks for `cli-plugins/` and `contexts/`, then `PATH=/tmp/no-keychain-bin:$PATH DOCKER_CONFIG=/tmp/docker-nokeychain docker compose build`. Ephemeral (/tmp) — recreate per session or promote to a script if headless builds become routine.
- **`offload.sh` / `offload_to_minimax.sh` invocation is still manual** — the tiered dispatcher (`scripts/offload.sh`) and its n8n-based watcher (`scripts/offload_watch.py` + `n8n/workflows/offload-watcher.json`) exist now (see Done), but dispatching a job still requires Claude to trigger it via Bash; no scheduled/event trigger chain yet. `offload_to_minimax.sh` itself is legacy — superseded by `offload.sh`'s tiered cline-pass/opencode-go routing.
- **`setup.sh`'s interactive path is untested** — first run bypassed its `.env` prompt (non-interactive session); should be run end-to-end once by a human to confirm the prompt/validation flow works.
- **`offload.sh` dispatch is still manual** — the tiered dispatcher (`scripts/offload.sh`) and its watcher (`scripts/offload_watch.py` + `n8n/workflows/offload-watcher.json`) exist now (see Done), but dispatching still requires Claude to run it via Bash; no scheduled/event trigger chain yet. `offload_to_minimax.sh` is legacy/superseded by `offload.sh`'s cline-pass/opencode-go routing.
- **Text-only restriction on NevNew's persona is prompt-level only** — the system prompt asks the model not to return media, but nothing enforces it at the proxy level (the backend models are multimodal-capable).
- **Docker Desktop crashed 3x in one session (2026-09-05)** — root cause was the Mac's disk hitting 94% full (1.2GB free), which broke the Docker VM's engine each time. Fixed by clearing npm/npx caches and removing Android Studio (~11GB reclaimed). Keep an eye on disk space going forward — Docker Desktop degrades badly (hangs, "unable to start", corrupted daemon responses) well before macOS itself complains.
- **`open-webui/custom.css` is pinned against Open-WebUI v0.11.3's DOM** — confirmed selectors: `#send-message-button`, `#new-chat-button`, `#model-selector-model-button`. `docker-compose.yml`'s image tag is now pinned to `v0.11.3` (was floating `:main`) specifically because of this. Re-verify these selectors live (via browser) before ever bumping that tag.
- **`scripts/backup.sh` is local-only and unrotated** — the Open-WebUI archive alone is ~963MB (mostly a cached embedding model, not chat text). Running this on a recurring schedule (e.g. an n8n cron job, matching the pattern already used for slowlife-game) without rotation or off-box copying will slowly refill the disk that was just at 94% full tonight. Needs either pruning old backups or a cloud-upload step before it's safe to automate.
- **OpenRouter's free tier is capped account-wide at 50 req/day (not per-model)** — discovered 2026-09-05 when all 3 of NevNew's fallback deployments (issue #7) 429'd simultaneously. The multi-model fallback only protects against one model being individually throttled; it does nothing for the shared daily cap, since all 3 draw from the same OpenRouter account/key. The `cline` CLI fallback (see Done below) papers over this for chat replies, but the underlying gap in issue #7's fallback strategy is still open.
  **Decided 2026-09-05**: not fixing via the $10 OpenRouter credit bump. Plan instead is to add the user's OpenCode Go quota (comes back 2026-09-06) as NevNew's long-term fallback/model source once it's available — see issue #7 comment. Revisit once OpenCode Go is back; until then the `cline` fallback covers the gap.
- **Proactive briefing engine (#65) needs two manual steps before it's live** — code shipped (see Done below) but the workflow ships with `"active": false` by design; a human must (1) grant Calendar/Reminders automation access to n8n/mcpo in System Settings (the #125 TCC gap — brief degrades to "calendar unverified" until then) and (2) install the two cron triggers (n8n's ExecuteCommand hangs Python in its task-runner sandbox, so the schedule runs from system cron instead of the n8n workflow itself). Exact commands and verification steps: `docs/SPRINT-2026-09-15.md`.

## Done

- **Telegram voice loop OGG/Opus conversion close-out (2026-09-15, issue #61)** — the STT/TTS round trip itself landed earlier (`043bde3`/PR #128: `handle_voice` in `telegram-bot/bot.py`, Groq Whisper STT + gTTS TTS, three-tier fallback to text), but the issue stayed open because that pass skipped the format conversion the acceptance criteria require. Added `_run_ffmpeg()` + `_ogg_to_wav()` (incoming voice note -> 16kHz mono WAV before Whisper) + `_mp3_to_ogg_opus()` (gTTS MP3 -> OGG/Opus so replies send as a native Telegram voice bubble via `send_voice` instead of a generic `send_audio` attachment). Each conversion degrades gracefully on failure (falls back to the unconverted bytes / the previous MP3-attachment behavior / plain text) rather than dropping the reply. `ffmpeg` added to `telegram-bot/Dockerfile`'s apt-get install (kept in the final image, unlike the purged `curl`/`gnupg`). No new env vars — `GROQ_API_KEY` was already in `.env.example`. Requires a `telegram-bot` container rebuild before it's live; see `docs/SPRINT-2026-09-15-voice.md` for known limitations (Thai accuracy unbenchmarked, gTTS has no SLA) and manual verification steps.
- **Proactive briefing engine (#65) — code complete (2026-09-15)** — `scripts/briefing.py` (`brief` mode: Open-Meteo weather + ai-core-composed calendar/reminders/memory brief pushed to Telegram at 07:00; `nudge` mode: 15-min event-starting-in-30-min check, exactly-once dedupe via `scripts/.briefing_state.json`, 23:00-07:00 quiet hours) shipped in `a575c5a`/PR #127 along with the underlying mcpo `base_url` fix that was silently breaking every tool call (`ai-core/nevnew_ai_core/tools/mcpo.py`) and `n8n/workflows/proactive-briefings.json` (kept `"active": false`, real trigger is system cron — n8n's ExecuteCommand hangs Python in its sandbox). This pass added `.env`-overridable weather location (`NEVNEW_HOME_LAT`/`NEVNEW_HOME_LON`, was hardcoded to Bangkok) and removed a stray emoji from the nudge copy. Still needs a human for the #125 Calendar/Reminders TCC grant and the cron install — see the Open-section entry above and `docs/SPRINT-2026-09-15.md` for exact commands and acceptance-criteria status.
- **Self-heal loop + menu bar health widget (2026-09-14)** — ops tooling lives OUTSIDE the repo (same rule as the watchdog): `~/ops/nevnew_selfheal.py` + launchd `com.nevnew.selfheal` (300 s). Probes 12 services (open-webui/litellm/mcpo/memory/ai-core endpoints, n8n via launchd `com.n8n.local`, postgres/redis/telegram-bot/cloudflared×2 by container state+healthcheck, qdrant `/healthz`) and remediates via a ladder: `docker start` → `docker restart` → scoped `docker compose up -d <svc>` (with cwd) → `launchctl kickstart` (n8n) → `open -a Docker` (daemon). 300 s per-service remediation cooldown (no restart storms); 3 consecutive failed runs files a GitHub issue once per episode (label `bug`). Writes `~/ops/nevnew-status.json` consumed by the menu bar widget `~/ops/nevnew-widget/NevNewStatus` (swiftc/AppKit NSStatusItem, no Xcode; launchd `com.nevnew.widget`, KeepAlive) — icon encodes overall health (stale >15 min), menu lists per-service ✓/✗ + detail, "Run self-heal now", service links, log. Gotchas baked in: n8n `/health` is a false positive (SPA catch-all 200s browser-like requests, API 404s — use `/healthz`; watchdog probe fixed too), `docker inspect {{.State.Health.Status}}` exits 1 without a `{{if .State.Health}}` guard on healthcheck-less containers, and `docker compose up -d` needs the compose dir as cwd under launchd. Also fixed stale post-volume-move paths: watchdog `COMPOSE_DIR` and `com.nevnew.backup` plist (was exit 126). Remediation ladder verified live with a throwaway container; all 12 services healthy. `newnew-n8n` compose container stays Exited by design (shadow of the launchd n8n).

- **Sprint 7 (2026-09-14): One Brain cutover (#70 closed)** — Telegram text routes via ai-core /chat (namespaced id, extraction on), CLI fallback tiers, /reset clears mem0, typing indicator (#71 closed on live traffic: 2 msgs ~3.5s/~1.2s, mem0 growing). PR #124 merged, bot rebuilt + recreated USE_AI_CORE=true, mem0 loop verified live. See `docs/SPRINT-2026-09-14-2.md`.
- **Sprint 6 (2026-09-14): close the loop + One Brain prep** — landed `e42a786` (single-provider 9arm chain, cost-based-routing, Redis cache); closed #74 (bot+cloudflared stable, restarts=0), #122 (Haiku 404 → haiku-4.5 swap, verified live), #121 (401-probe noise, KNOWN_NOISE widened); triage-swept 17 stale auto-bugs (#47–#52, #56–#60, #83–#88); merged #70 prep PR #123 (design note + inert `_run_aicore()` plumbing, no cutover). Tracker ~50 → 29 open. Lesson: offload workers can't do gh-backed triage from scratch cwd (no repo context) — paste bodies into the task or keep triage with the orchestrator. See `docs/SPRINT-2026-09-14.md`.

- **Telegram bot chat path rewritten (2026-09-09): opencode primary → claude CLI Sonnet fallback** — LiteLLM/cline are no longer in the bot's chat path at all (LiteLLM still serves Open-WebUI). Primary: `opencode run` on the OpenCode Go quota (`opencode-go/glm-5.3`), isolated scratch cwd, no auto-approve. Fallback: `claude -p` with Sonnet via `CLAUDE_CODE_OAUTH_TOKEN` (separate quota), `--allowedTools ""` fail-closed. Both agent-CLI outputs pass a `_gate_agent_reply` chat gate (rejects tool-call attempts/empty output, strips markdown images, caps length). Owner commands `/start` + `/reset` to manage history. Bot history rollback on failed turns so recorded history stays in sync with what the model saw.
- **Telegram photo → vision OCR reply (verified live 2026-09-10)** — `handle_photo` in `telegram-bot/bot.py`: owner-sent photos are downloaded and sent as base64 to `google/gemini-2.5-flash` (env-tunable via `OPENROUTER_VISION_MODEL`) through OpenRouter directly — deliberately bypassing the agent CLIs, which have no non-tool way to ingest an image. System prompt = persona + a suffix instructing verbatim transcription of visible text, then a persona-voiced comment on the image. Non-owner photos are ignored. Graceful "couldn't read that image" reply on failure; history rollback.
- **Tiered offload dispatcher + watcher (2026-09-06)** — `scripts/offload.sh`: task classes small/code/complex map to cline-pass workers (qwen3.7-plus / deepseek-v4-pro / mimo-v2.5-pro) with `opencode-go` and `--via openrouter` fallback routes; workers run in an isolated scratch cwd with no auto-approve, output extracted as text, orchestrator stays the quality gate. `scripts/offload_watch.py` + the n8n "NevNew Offload Watcher" workflow (1-min schedule + webhook trigger) watch `/tmp/nevnew-offload-jobs.json` and Telegram-notify on state transitions, exactly once per change. (Also: the n8n Mac-host tools pattern from #1c48db8 — Reminders/Calendar/Notes via `n8n/macos_tools.applescript` + mcpo; GitHub tools via `scripts/gh_tool.sh` allowlist wrapper, issue #5.)
- **Error → autofix → review pipeline (2026-09-06)** — `scripts/watch_and_autofix.sh` chains `error_watcher.py` into `scripts/autofix_with_cline.sh` (dispatches each newly filed bug issue to a deepseek-v4-pro worker that opens an `auto-fix`-labeled PR, never merges itself), and `scripts/review_and_merge_autofix_prs.sh` is the headless Claude review gate that merges clean auto-fix PRs + rebuilds/healthchecks affected containers, leaving murky ones open with a blocking comment.

- **Telegram bot (issue #11) wired up** — `telegram-bot/` (bot.py + Dockerfile), new `telegram-bot` service in `docker-compose.yml`. Owner-only whitelist, in-memory per-user history (last 20 messages, resets on restart), forwards to LiteLLM (`model=NevNew`, persona applied via the existing callback, no duplication), graceful "having technical trouble" reply on any LiteLLM/network failure. Verified live 2026-09-05: bot started, round-tripped a real message through LiteLLM, replied. Follow-up if needed: history doesn't survive a restart — fine for MVP per issue #11's scope.
- **Telegram bot: `cline` fallback on OpenRouter 429s** — when LiteLLM 429s (see OpenRouter daily-cap gap above), the bot now shells out to the `cline` CLI (`telegram-bot/bot.py: _run_cline`/`_gate_cline_reply`) using the already-authenticated Cline account mounted in from the host (`~/.cline` → `/cline-home`, a separate quota pool from the OpenRouter key). Locked down since `cline` is a full coding agent, not a chat API: `--auto-approve false`, empty scratch cwd, persona reused from `callbacks/nevnew_persona_prompt.py` (factored out so telegram-bot can import it without pulling in LiteLLM), and every reply passes through a chat-gate (rejects empty output, anything where cline attempted a tool call, strips stray markdown-image syntax, caps to Telegram's message length) before it can reach the user. Requires Node 20 + the `cline` npm package in the container image (`telegram-bot/Dockerfile`). Known cline CLI quirk worked around: a bare single-word prompt (e.g. `"hi"`) makes cline's own arg parser bail with "Unknown command or unquoted prompt" — fixed with a leading-space prefix on the prompt passed to cline.
- **`scripts/error_watcher.py` — auto-detect real errors → file GitHub bug issues** — tails all 4 containers' docker logs (state file tracks last-checked timestamp per container so cron runs only see new lines), dedupes by a normalized signature so the same error only files once, and skips a `KNOWN_NOISE` allowlist of expected/operational messages (currently: OpenRouter's daily-cap message, and the bot's own log line for it) so routine rate-limiting doesn't spam the tracker. Files via `gh issue create --label bug`. Detection only — no auto-fix — by design (see issue discussion 2026-09-05). Installed in crontab, every 20 min (marker `nevnew_error_watcher`). Verified live 2026-09-05: correctly caught a real bug (cline's single-word-prompt parser quirk, above) as issue #14, and initially over-fired once on a litellm traceback that was actually the known daily-cap noise (issue #13, closed) — fixed by widening the noise-matching window from 3 to 30 log lines, since litellm's exception-chaining preamble can put 10+ lines between the informative ERROR line and a later bare `Traceback (most recent call last):` header for the same incident.
- Girly light theme (pink accents) + hidden model-selector dropdown — `open-webui/custom.css`, mounted read-only at `/app/build/static/custom.css`, verified live in Chrome on 2026-09-05.
- Chat backend swapped `musespark-1.3` (fabricated endpoint, never worked) → `google/gemma-4-31b-it:free` → `z-ai/glm-5.2:free` (current primary) — 2026-09-05.
- **Sprint 0** (reliability/cost foundation) — GitHub issues #7-#10, all closed 2026-09-05: multi-model fallback (3 free deployments under `NevNew`, cooldowns re-enabled), self-imposed `rpm: 20` per deployment, confirmed existing docker logs are adequate observability for now (all models free, nothing to spend-track yet), local backup script (see caveat above).
- Fixed a real bug in `scripts/offload_to_minimax.sh`: it called OpenRouter's API directly but used a LiteLLM-style `openrouter/` model prefix, which OpenRouter doesn't understand. Fixed to the correct raw id (`minimax/minimax-m3:free`).
