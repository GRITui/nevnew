# Sprint 4 — 2026-09-20 ("Delegate to build")

Carry-over sprint: land the remaining Sprint-3 stabilization P0s, then restart feature
work with **cheap-worker delegation as the default execution path**. Small, well-scoped
tasks go to `qwen3.8-27b-fp8` (workhorse) and `cohere/north-mini-code:free` (light
triage); the orchestrator stays the quality gate before anything reaches `main`.

Squad: orchestrator + worker pool (`qwen3.8-27b-fp8`, `north-mini-code:free`).
Milestone: `Sprint 4` · Label: `sprint-4` · Board: https://github.com/users/GRITui/projects/7

## Worker pool (validated live 2026-09-13)

| Model | Route | Quota | Best at |
|-------|-------|-------|---------|
| `qwen3.8-27b-fp8` | 9arm gateway `/v1` (`openai-compatible`, `NINEARM_API_KEY`) | unmetered, own gateway, `rpm: 20` | code-gen, config fixes, diagnosis — **default worker** |
| `cohere/north-mini-code:free` | OpenRouter free tier (`OPENROUTER_API_KEY`) | **50 req/day account-wide** | light triage, doc snippets, one-shot summaries |

- Both smoke-tested 2026-09-13: qwen → `200` (emits `reasoning_content`; disable
  reasoning for plain codegen to save tokens); north-mini → model exists, `429`
  day-cap currently active (resets daily).
- **Caveat:** OpenRouter's 50/day cap is **account-wide, not per-model** (BACKLOG note).
  north-mini is a 4th free deployment but does **not** raise the shared cap — use it
  for tiny tasks only; qwen3.8 on the 9arm gateway is the real workhorse.
- Gateway `/models` currently serves **only** `qwen3.8-27b-fp8`.

## Enabler first · offload routing to the new workers

~~Update `scripts/offload.sh` `class_spec()` + `--via` routes~~ — **DONE 2026-09-13,
commit `e910320`** (with adjustments vs the original spec in #118):

- Landed as `--via openai` (generic OpenAI-compatible endpoint), **promoted to the
  default route** (`OFFLOAD_VIA` overrides) — not `--via gateway`. #118 updated, no rename.
- Model **pinned to `qwen3.8-27b-fp8` for all classes** (gateway allowlist is exactly
  that model; verified live — anything else 403s). Tiering lives on `--via cline` only.
- `small`/openrouter → `cohere/north-mini-code:free` (verified live same day — the
  50/day cap had reset).
- Also fixed the `THINKING` off-by-one found during implementation.
- Verified: `bash -n` clean, exact-echo round-trips on all classes, fail-closed (exit 1)
  on bad model. Dogfooding rule from here on: worker-sized tasks go through
  `./scripts/offload.sh` (no flag = openai route), orchestrator QA-gates every diff.
- Remaining: close #118 after one real (non-echo) task round-trips through the route.

## Goals

| Pri | Issue | Task | Worker model |
|-----|-------|------|--------------|
| P0 | #117 (carry-over) | LiteLLM↔Postgres reconnect — orchestrator-led (infra debugging, not worker-sized) | — |
| P0 | #81 (carry-over) | mcpo: diagnose + prepare fix + verify (n8n toggle is user-blocked) | qwen3.8 (diagnosis) |
| P1 | #66 | ai-core `/ready` false negative → real health endpoint | qwen3.8 (code) |
| P1 | #89–#91 | Triage stale telegram-bot auto-bugs (old opencode path) → close | north-mini (triage) |
| P2 | #118 | offload.sh enabler — DONE 2026-09-13 (`e910320`); close after 1 real task round-trips | — (dogfood) |
| P2 | #37 | Mem0+Qdrant memory restart — worker drafts, orchestrator integrates | qwen3.8 (draft) |

## Sprint started — 2026-09-13 log

- **#118 CLOSED:** first real (non-echo) round-trip done — worker triaged #89/#90/#91
  via the default openai route, orchestrator QA-gated and acted (#89 stale-close:
  opencode-primary path no longer in bot.py; #90/#91 dupes of #117). Enabler complete.
- **#117 FIXED + CLOSED (orchestrator-led):** root chain was disk-full PANIC (2026-09-12
  16:11) → postgres restarted with corrupted network attachment (`invalid IP`, stale
  daemon endpoint) → Prisma `No route to host`, 1620+ consecutive failures. Fix:
  `compose down` + force-disconnect orphaned endpoints + fresh `up -d`. Verified:
  TCP_OK litellm→postgres, zero reconnect failures, migrations deployed, /v1/models
  serves both models. Note: `/health/liveliness` returned 200 throughout — it does not
  exercise the DB path.
- **#81 FIXED + CLOSED:** root causes — (a) mcpo pointed at the workflow ID as if it
  were the trigger path (correct prod URL: `/mcp/b7e2d1a4…`); (b) TWO n8n instances
  exist, Mac-local (launchd, holds :5678, live) vs compose container (shadowed,
  empty); (c) Mac workflow active but missing webhook rows — reactivation + service
  restart wrote them, endpoint 200. Compose repointed (`59183a7`), mcpo restarts=0,
  healthy, 12 tools. Note: old MCP Spike still Published (live 4f0aba37) — consider
  unpublishing. Browser UI showed stale cache mid-debug — hard-refresh when in doubt.
- **Monitor installed:** `scripts/sprint_monitor.sh` hourly (`nevnew_sprint_monitor`
  cron) watches (1) open DB-signature auto-issue count (recurrence = #117 back or
  widen watcher dedupe) and (2) gateway `/models` allowlist (change = revisit qwen pin).
  Baseline 2026-09-13: db_issues=0, models=[qwen3.8-27b-fp8]. Log:
  `/Users/grit/ops/nevnew-sprint-monitor.log`.
- Tracker: 77 → ~50 open. Next: #66 via worker, #37 only after 24h quiet.

- **Triage:** bulk-closed 23 auto-filed dupes (#92–#116) as duplicates of #117 — all one
  signature (LiteLLM→Postgres "All connection attempts failed", incident window
  2026-09-12). Tracker went 77 → 54 open. Watch: if error_watcher keeps filing the same
  signature while #117 is open, consider widening its dedupe/quiet window for that
  signature instead of closing by hand again.
- **Decisions:** keep `--via openai` name (update #118, no rename); wire north-mini now
  rather than defer; close dupes now rather than after #117; commit enabler on
  `feat/9arm-gateway-chat-engine` (not pushed).
- **#89–#91 note for triage:** #90/#91 (search_tool_registry, MCP reload from DB) read
  like #117/#81 symptoms — likely close as blocked-by/dupes once those land. #89
  (telegram-bot "both fallbacks failed") may be stale relative to the cline-primary bot —
  verify against current code before closing.

## Worker-size rule of thumb

- **Delegate (qwen3.8):** single-file codegen, config/healthcheck edits, log
  diagnosis, doc snippets, test scaffolding.
- **Delegate (north-mini):** triage + close stale issues, short summaries, labels.
- **Do NOT delegate:** multi-service debugging (#117), anything needing live
  container state or secrets, anything touching `.env`/credentials, final QA.

## Sequencing

1. **offload routing** (enabler) — ~~verify a real delegated task round-trips~~ DONE
   (`e910320`); still needs 1 real (non-echo) task through it to close #118.
2. **P0 carry-over:** #117 (orchestrator) + #81 (worker diagnosis) in parallel.
3. **P1 via workers:** #66 fix, #89–#91 triage (see planning-log notes — expect dupes).
4. **QA gate** on every worker-produced diff before it touches the repo.
5. **Feature restart:** #37 only after P0s land and the stack is quiet 24h.

## Definition of done

- offload.sh routes live + ≥1 real task delegated through them (proof in log/PR).
- #117, #81, #66 closed; #89–#91 triaged.
- No new auto-filed reliability bugs for 24h.
- All worker diffs passed the orchestrator QA gate (no secrets, configs valid).

## Model policy (current)

- **telegram-bot primary:** Cline CLI → `openai-compatible` / `qwen3.8-27b-fp8` via
  `https://gateway.9arm.co/v1` (Claude Sonnet fallback retained).
- **Open-WebUI:** LiteLLM `NevNew` (9arm gateway) / `NevNew-Pro` (OpenRouter, capped).
- **Workers:** qwen3.8 (default) + north-mini-code:free (light) via offload.sh.
- Never commit `.env`, `.env.save`, `.env.local` (gitignored).
