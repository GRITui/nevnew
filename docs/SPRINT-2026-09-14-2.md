# Sprint 7 — 2026-09-14 ("One Brain cutover")

Executed 2026-09-14. Scope: #70 cutover + #71 latency check.

## Log

- **#70 CUTOVER live, PR #124 merged**: `handle_message` routes Telegram
  text via ai-core `POST /chat` (user_id `telegram:<id>`, channel
  telegram, `store_memories=true`) with typing indicator; cline → claude
  CLI tiers on ai-core failure; `/reset` best-effort clears mem0 +
  local history. Compose `USE_AI_CORE` defaults true (rollback: false +
  recreate). `.env.example` documents ai-core keys, marks OAuth legacy.
- **Verified end-to-end live**: direct `/chat` → reply `cutover-ok`
  (5.78s, iterations=0, 1 round trip); background extraction ADDed 2
  mem0 facts to per-user collection, visible via ai-core `/memory`
  proxy. Bot rebuilt (keychain workaround), recreated, `USE_AI_CORE=true`
  in live env, polling clean zero errors.
- **#70 CLOSED** (auto-closed by merge + evidence comment). Leftover
  future: cross-channel user_id mapping table (Open-WebUI ids differ).
- **#71 CLOSED on live traffic**: owner confirmed the bot works; logs show
  2 messages both on the ai-core path (~3.5s, ~1.2s — under 5s), typing
  indicator sent, mem0 retrieval growing 0→2→3 across turns.
- QA each step: py_compile, compose config, secret scan clean.

## Next

- Confirm #71 p50 from live use, then close.
- MVP queue: #61 voice, #65 briefing. Reliability: #73, #78, #80, #63, #68.
- Carry-overs: #36, #38, #39, #40. #37 if 24h quiet holds.
