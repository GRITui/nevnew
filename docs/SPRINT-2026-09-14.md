# Sprint 6 — 2026-09-14 ("Close the loop then One Brain prep")

Executed 2026-09-14. Scope per user pick: Stabilize + One Brain prep,
push-first, worker triage (orchestrator QA-gates).

Label: `sprint-5` carry + new · Board: https://github.com/users/GRITui/projects/7

## Starting position (from Sprint 5 doc)

Branch merged (#119), #66 fixed (#120), #117/#81/#118 done. Carried:
unpushed `e42a786`, #74 open, #70 prep not started, ~20 stale auto-bugs.

## Log

- **Pushed `e42a786`** (direct, was on main — no PR needed): single-provider
  9arm chain, cost-based-routing + Redis cache, Groq fallback, Mini/Haiku
  lanes. QA: YAML valid, `bot.py` compiles, `compose config` clean,
  no secrets.
- **#74 CLOSED** with evidence: bot Up 7h restarts=0 polling clean,
  cloudflared Up 10h restarts=0, `getMe` 200.
- **#122 CLOSED** (Haiku 404 fixed by `e42a786`'s 3.5-haiku → haiku-4.5
  swap, verified live `haiku-ok`). **#121 CLOSED** as noise + widened
  `KNOWN_NOISE` (commit `728fd70`, pushed).
- **Triage sweep: 17 closed** (#47–#52, #56–#60, #83–#88).
  Worker attempt via `offload.sh -c small` FAILED QA (speculated without
  reading issues — rejected); orchestrator triaged directly from issue
  bodies + live state. Dispositions: #87/#88 stale (old opencode path
  gone); #84-86, #49-52, #56/#57 dupe-of-#81; #58/#59 dupe-of-#74;
  #83 dupe-of-#121; #48 dupe-of-#117; #60 stale (one-off Groq ITPM);
  #47 stale (minimax free pulled).
- **#70 prep landed, PR #123 merged**: `docs/ONE-BRAIN-70.md` design note
  + `_run_aicore()` plumbing (inert, `USE_AI_CORE=false`, zero call
  sites). Cutover explicitly future. Noted on #70, left open.
- **Monitor**: `db_issues=0`, `gateway_models=[qwen3.8-27b-fp8]` — green.
  All core containers Up/healthy. Tracker ~50 → 29 open.

## Worker note

north-mini via offload.sh is unreliable for gh-backed triage (scratch cwd
has no repo/gh context — it guessed). Rule update: worker triage tasks
must be file-local (paste issue bodies into the task file) or stay with
the orchestrator.

## Remaining for next sprint

- #70 cutover (handler wiring, user_id mapping, CLI migration into ai-core).
- #71 latency (follows #70's route).
- MVP queue: #61 voice, #65 briefing. Reliability: #73, #78, #80, #63, #68.
- Carry-overs: #36, #38, #39, #40. #37 memory restart if 24h quiet holds.
