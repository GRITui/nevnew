# Sprint 5 — 2026-09-27 ("Stabilize then One Brain")

Light-week close-out sprint: land the remaining Sprint-4 leftovers, sweep the
stale auto-bug backlog, then start **One Brain (#70) with prep only** (design
note + smallest prep PR — no cutover). Cheap-worker delegation stays the
default execution path per Sprint 4's dogfooding rule.

Squad: orchestrator + worker pool (`qwen3.8-27b-fp8`, `north-mini-code:free`).
Label: `sprint-5` · Board: https://github.com/users/GRITui/projects/7

## Starting position (from Sprint 4 log, 2026-09-13)

Done: #117 (Postgres reconnect), #81 (mcpo endpoint), #118 (offload enabler +
real round-trip), #89–#91 triaged, #92–#116 bulk-closed as dupes.
Tracker ~77 → ~50 open. Monitor installed (`scripts/sprint_monitor.sh` hourly,
log at `/Users/grit/ops/nevnew-sprint-monitor.log`).

Leftovers carried in:
- Branch `feat/9arm-gateway-chat-engine` **2 commits ahead, unpushed**
  (`59183a7` mcpo fix, `e910320` offload routes) + uncommitted
  `docs/SPRINT-2026-09-20.md` log + untracked `scripts/sprint_monitor.sh`.
- #66 open (ai-core `/ready` false negative, sprint-3 + sprint-4).
- #74 open (telegram-bot + cloudflared stale-env crash-loop — fix described in
  Sprint 3, never verified closed).
- #37 open (Mem0+Qdrant memory restart, sprint-4 P2, gated on "24h quiet").
- Stale auto-bugs still open: #47–#60, #83–#88 (litellm/mcpo/bot-network/
  open-webui-tools — mostly pre-#117/#81 incident window, likely dupes).

## Goals

| Pri | Issue | Task | Worker model |
|-----|-------|------|--------------|
| P0 | — | Push + PR + merge `feat/9arm-gateway-chat-engine` (incl. sprint log + `sprint_monitor.sh`); monitor green 24h | orchestrator (short, secrets-adjacent) |
| P0 | #66 | ai-core `/ready` → real health endpoint (currently pings 404 `/v1/health/liveliness`) | qwen3.8 (code) via `offload.sh` |
| P0 | #74 | Verify bot+cloudflared stable on current env handling; close with evidence or re-scope | orchestrator verify + worker doc touch if needed |
| P1 | #47–#60, #83–#88 | Triage sweep: dupe-of #117/#81 / stale-close, keep only still-reproducing | north-mini (triage) via `offload.sh` |
| P1 | #70 (prep only) | Design note + smallest prep PR: Telegram → ai-core route with persistent history sketch; **no cutover** | qwen3.8 (draft) |
| P2 | #37 | Deferred unless 24h quiet already met at sprint start; else carries to Sprint 6 | — |
| — | #71 | Explicitly deferred (latency work follows #70's route, not before) | — |

## Out of scope (stays open)

- **MVP:** #61 (voice), #65 (briefing) — queued behind #70.
- **Reliability:** #73 (security/LAN exposure), #78 (backup gaps), #80 (image
  pins), #63 (disk alerts), #68 (watcher latency).
- **Sprint-2 carry-over:** #36 (MCP connectors), #38 (omnichannel), #39 (ai-core
  harness), #40 (tunnel + budget webhooks).
- Rationale: light week — land stability + prep, don't open new fronts.

## Sequencing

1. **Land the branch** — push, AI-review PR, squash-merge. Unblocks everything
   else; clears the "ahead by 2" risk.
2. **#66 via worker** — single-file fix in `ai-core/`; healthcheck reflects
   litellm + memory deps; verify `GET /ready` returns 200.
3. **#74 verify** — `docker ps` stable + `getMe` 200; close with evidence or
   convert to an env-validation task.
4. **Triage sweep** — one worker pass over #47–#60 + #83–#88; orchestrator
   bulk-acts (expect 80%+ dupe/stale closes).
5. **#70 prep only** — design note (route, history store, rollback) + at most
   one small prep PR behind the existing bot path (no cutover).
6. **QA gate** — every worker diff reviewed before it touches the repo.

## Definition of done

- Branch merged; `main` clean; monitor log shows 24h with `db_issues=0`.
- #66, #74 closed with verification evidence.
- Auto-bug backlog triaged (each of #47–#60, #83–#88 dispositioned).
- #70 has a merged design note + optional prep PR; cutover explicitly **not**
  in this sprint.
- No new auto-filed reliability bugs for 24h at sprint end.

## Worker-size rule of thumb (from Sprint 4, unchanged)

- **Delegate (qwen3.8):** single-file codegen, config/healthcheck edits, log
  diagnosis, doc snippets, test scaffolding.
- **Delegate (north-mini):** triage + close stale issues, short summaries, labels.
- **Do NOT delegate:** multi-service debugging, anything needing live
  container state or secrets, anything touching `.env`/credentials, final QA.

## Model policy (current)

- **telegram-bot primary:** Cline CLI → `openai-compatible` / `qwen3.8-27b-fp8` via
  `https://gateway.9arm.co/v1` (Claude Sonnet fallback retained).
- **Open-WebUI:** LiteLLM `NevNew` (9arm gateway) / `NevNew-Pro` (OpenRouter, capped).
- **Workers:** qwen3.8 (default) + north-mini-code:free (light) via offload.sh.
- Never commit `.env`, `.env.save`, `.env.local` (gitignored).

## Risks / watch items

- The hourly monitor is the 24h-quiet oracle for #37 — if it fires, #37 slips
  again by design.
- Old MCP Spike workflow still Published (noted in Sprint 4 log) — consider
  unpublishing during the #74 verify.
- `open-webui:v0.11.3` pin + `custom.css` selectors — do not bump image tags
  this sprint (deferred #80).
